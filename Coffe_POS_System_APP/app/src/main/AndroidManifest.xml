<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPOSAPPLICATION"
        tools:targetApi="31">

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- BookingActivity -->
        <activity
            android:name=".BookingActivity"
            android:exported="true">
        </activity>

        <!-- TableSelectionActivity (với activity cha được xác định) -->
        <activity
            android:name=".TableSelectionActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- RegisterActivity (được thay thế SignUpActivity) -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".Cart"
            android:exported="true">
        </activity>
        <activity
            android:name=".Create_invoice_cart"
            android:exported="true">
        </activity>
        <activity
            android:name=".Profile"
            android:exported="true">
        </activity>

    </application>

</manifest>
